const synonyms:Record<string,string>={caravana:"internalId",id:"internalId",idanimal:"internalId",animalid:"internalId",caravanaelectronica:"eid",eid:"eid",rfid:"eid",peso:"weightKg",pesokg:"weightKg",kg:"weightKg",fecha:"date",fechapesaje:"date",raza:"breed",sexo:"sex",machohembra:"sex",precio:"unitPrice",preciounitario:"unitPrice",gs:"unitPrice",comprador:"buyerName",cliente:"buyerName"};
const key=(value:string)=>value.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]/g,"");
export function autoMapHeaders(headers:string[]){return Object.fromEntries(headers.map(header=>[header,synonyms[key(header)]||null]));}
export function parseNumber(value:unknown){if(typeof value==="number")return value;if(value===null||value===undefined||String(value).trim()==="")return null;let text=String(value).replace(/\s|kg|gs\.?/gi,"");if(text.includes(",")&&text.includes(".")){text=text.lastIndexOf(",")>text.lastIndexOf(".")?text.replace(/\./g,"").replace(",","."):text.replace(/,/g,"");}else if(text.includes(",")){text=text.replace(",",".");}else if(/^\d+\.\d{3}$/.test(text)){text=text.replace(".","");}const number=Number(text);return Number.isFinite(number)?number:null;}
export function parseDate(value:unknown){if(value instanceof Date)return value;if(typeof value==="number")return new Date(Date.UTC(1899,11,30)+value*86400000);const match=String(value??"").trim().match(/^(\d{1,2})[/-](\d{1,2})[/-](\d{4})$/);if(match)return new Date(Date.UTC(Number(match[3]),Number(match[2])-1,Number(match[1])));const iso=new Date(String(value));return Number.isNaN(iso.getTime())?null:iso;}
export function normalizeSex(value:unknown){const normalized=key(String(value??""));if(["m","macho","male"].includes(normalized))return "MALE" as const;if(["h","hembra","female"].includes(normalized))return "FEMALE" as const;return null;}
export function validateWeight(value:unknown){const weight=parseNumber(value);return weight!==null&&weight>0&&weight<=1500?weight:null;}
