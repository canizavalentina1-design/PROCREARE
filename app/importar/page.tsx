"use client";
import { useState } from "react";
export default function ImportPage(){const [step,setStep]=useState(1);return <main className="shell"><aside className="sidebar"><div className="brand">PROCREARE</div><div className="farm">Finca principal<span>Importar</span></div><nav><a href="/">Inicio</a><a href="/animales">Animales</a><a href="/pesajes/lote">Pesajes</a><a href="/ventas">Ventas</a><a className="active" href="/importar">Importar</a><a href="#ajustes">Ajustes</a></nav></aside><section className="content"><header><div><div className="eyebrow">CARGA SEGURA DE DATOS</div><h1>Importar</h1></div><span className="muted">Paso {step} de 5</span></header><div className="surface"><h2>{step===1?"Elija qué desea importar":step===2?"Seleccione un archivo":step===3?"Revise las columnas":"Vista previa"}</h2><p className="muted">Nada se guarda en sus animales, pesajes o ventas hasta que usted confirme.</p>{step===1&&<div className="empty-actions"><button className="button" onClick={()=>setStep(2)}>Animales</button><button className="secondary" onClick={()=>setStep(2)}>Pesajes</button><button className="secondary" onClick={()=>setStep(2)}>Ventas</button></div>}{step>1&&<><label>Archivo Excel o CSV<input type="file" accept=".xlsx,.csv" /></label><div className="empty-actions"><button className="secondary" onClick={()=>setStep(Math.max(1,step-1))}>Atrás</button><button className="button" onClick={()=>setStep(Math.min(5,step+1))}>{step===5?"Confirmar importación":"Continuar"}</button></div></>}</div></section></main>}
