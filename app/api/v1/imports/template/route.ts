import ExcelJS from "exceljs";
import { requireSession } from "../../../../../src/server/auth/session";
export const runtime="nodejs";
export async function GET(request:Request){try{await requireSession();const type=new URL(request.url).searchParams.get("type")||"ANIMALS";const headers=type==="WEIGHINGS"?["caravana","fecha","peso kg","tipo"]:type==="SALES"?["fecha","comprador","caravana","peso kg","precio"]:["caravana","raza","sexo","categoria","origen","fecha nacimiento"];const workbook=new ExcelJS.Workbook();const sheet=workbook.addWorksheet("Plantilla");sheet.addRow(headers);sheet.addRow(headers.map((header)=>header==="sexo"?"Hembra":header==="peso kg"?"350":header==="fecha"?"23/09/2026":"Ejemplo"));sheet.getRow(1).font={bold:true};const buffer=await workbook.xlsx.writeBuffer();return new Response(buffer,{headers:{"content-type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","content-disposition":`attachment; filename="procreare-${type.toLowerCase()}-plantilla.xlsx"`}});}catch(error){return Response.json({error:{code:error instanceof Error?error.message:"INTERNAL",message:"No se pudo generar la plantilla."}},{status:401});}}
