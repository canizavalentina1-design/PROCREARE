services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: procreare
      POSTGRES_PASSWORD: procreare
      POSTGRES_DB: procreare
    ports: ["5432:5432"]
    volumes: ["procreare_pg:/var/lib/postgresql/data"]
volumes:
  procreare_pg:
