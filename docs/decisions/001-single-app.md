# ADR 001: Single Next.js app

PROCREARE stays at the repository root as one Next.js App Router application so Vercel can detect it without a Root Directory override. Services and contracts remain separated inside `src/`.
